{
  "name": "pixeltrace",
  "version": "1.2.0",
  "private": true,
  "description": "Local, scale-tolerant visual fragment locator",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check app.js && node --check matcher-core.js && node --test tests/*.test.js"
  }
}
